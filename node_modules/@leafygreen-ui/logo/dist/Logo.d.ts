/// <reference types="react" />
import PropTypes from 'prop-types';
import { LogoProps, Product, Lockup } from './utils';
/**
 * # Logo
 *
 * React Component that displays MongoDB Logo.
 *
 * ```
<Logo />
```
 * @param props.darkMode Determines whether or not the <Logo /> will appear in dark mode.
 * @param props.knockout Boolean to describe whether or not knockout version of MongoDB logo will be used.
 * @param props.height Determines height of the <Logo /> component.
 */
declare function Logo({ darkMode, knockout, height, product, lockup, role, 'aria-label': ariaLabel, ...rest }: LogoProps): JSX.Element;
declare namespace Logo {
    var displayName: string;
    var propTypes: {
        darkMode: PropTypes.Requireable<boolean>;
        knockout: PropTypes.Requireable<boolean>;
        height: PropTypes.Requireable<number>;
        product: PropTypes.Requireable<Product[]>;
        lockup: PropTypes.Requireable<Lockup[]>;
    };
}
export default Logo;
//# sourceMappingURL=Logo.d.ts.map